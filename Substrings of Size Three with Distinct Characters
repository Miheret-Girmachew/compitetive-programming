class Solution:
    def countGoodSubstrings(self, s: str) -> int:
        count=0
        for indexx in range(len(s)-2):
            substring=set(s[indexx:indexx+3])
            if len(substring)==3:
                count=count+1
        return count
        
        
