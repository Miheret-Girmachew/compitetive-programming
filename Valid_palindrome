class Solution:
    def isPalindrome(self, s: str) -> bool:
        new=""
        for char in s:
            if char.isalnum():
                new+=char
        new2=new.lower()
        if new2 == new2[::-1]:
            return True
        else:
            return False
    
        
