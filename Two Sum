class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        sum={}
       
        for index, num in enumerate(nums):
            num_key= target - num
            
            if num_key in sum :
                return [sum[num_key], index]
            sum[num]= index

        return []
